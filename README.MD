#  Cashback test

This project was implemented using technologies JQuery,  Sass, Gulp, BEM.

#### `git clone https://github.com/VadimVelikanets/cashback.git`

#### `npm install`

#### `cd cashback`

#### `gulp start`

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

